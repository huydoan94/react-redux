import React from 'react';
import { shallow } from 'enzyme';
import { expect } from 'chai';

import Input from '../input.view';

describe('input view', () => {
    it('render a input', () => {
        // const inputWraper = shallow(<Input/>);
        // expect(inputWraper.find('input')).to.have.length(1);
    });
});